#save HTML
    file1 = open('./htmls/' + filename + ".html", 'w') #overwrite existing
    file1.write(driver.page_source.encode('utf-8'))
    file1.close()



or


    with open('./htmls/' + filename + ".html", 'r') as myFile:
        for num, line in enumerate(myFile, 1):
            if lookup in line:
		#Code here
